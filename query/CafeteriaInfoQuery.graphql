query CafeteriaInfoQuery($campusID: Int!, $date: Date!) {
  cafeteria(input: { campus: $campusID, date: $date }) {
    name, latitude, longitude, runningTime {
      breakfast, lunch, dinner
    }
  }
}