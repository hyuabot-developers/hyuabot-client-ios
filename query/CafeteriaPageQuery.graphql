query CafeteriaPageQuery($date: Date!, $campusID: Int!) {
    menu (date: $date, campusId: $campusID) {
        id, menu {
            type, menu, price
        }, runningTime {
            breakfast, lunch, dinner
        }
    }
}
